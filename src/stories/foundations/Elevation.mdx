import { Meta } from '@storybook/blocks';

<Meta title="Foundations/Elevation" />

# Elevation (Z-Axis)

Elevation is the relative distance between two surfaces along the z-axis. Elements with higher elevation values are visually distinguished by larger, softer shadows.

## Depth Model

In the Enterprise Design System, all elements base themselves on a 0dp surface level (background).

-   **Depths** are measured in `dp` (density-independent pixels).
-   **Shadows** are the only visual cue for depth (we do not use Z-axis translation).

## Light Mode Preview
<div className="p-12 rounded-xl mb-12 shadow-inner" style={{ backgroundColor: '#E2E8F0' }}>
  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
    {/* Shadow Cards (Light) */}
    {[
      { level: 0, shadow: 'none', label: 'No Shadow' },
      { level: 1, shadow: 'shadow-sm', label: 'Shadow SM' },
      { level: 2, shadow: 'shadow', label: 'Shadow' },
      { level: 3, shadow: 'shadow-md', label: 'Shadow MD' },
      { level: 4, shadow: 'shadow-lg', label: 'Shadow LG' },
      { level: 5, shadow: 'shadow-xl', label: 'Shadow XL' },
    ].map((item) => (
      <div key={item.level} className="flex flex-col items-center">
        <div className={`w-32 h-32 bg-white rounded-lg ${item.shadow} flex items-center justify-center text-xs text-slate-600 font-medium`}>
          {item.label}
        </div>
        <div className="mt-4 text-xs font-semibold text-slate-500 uppercase tracking-widest">Level {item.level}</div>
      </div>
    ))}
  </div>
</div>

## Dark Mode Preview
<div className="p-12 rounded-xl mb-12 shadow-inner" style={{ backgroundColor: '#0F172A' }}>
  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
    {/* Shadow Cards (Dark) */}
     {[
      { level: 0, shadow: 'none', label: 'No Shadow' },
      { level: 1, shadow: 'shadow-sm', label: 'Shadow SM' },
      { level: 2, shadow: 'shadow', label: 'Shadow' },
      { level: 3, shadow: 'shadow-md', label: 'Shadow MD' },
      { level: 4, shadow: 'shadow-lg', label: 'Shadow LG' },
      { level: 5, shadow: 'shadow-xl', label: 'Shadow XL' },
    ].map((item) => (
      <div key={item.level} className="flex flex-col items-center">
        {/* Slate-800 Surface on Slate-900 Background with Strong Black Shadows */}
        <div className={`w-32 h-32 rounded-lg ${item.shadow} shadow-black/80 flex items-center justify-center text-xs text-slate-300 font-medium`} style={{ backgroundColor: '#1E293B' }}>
          {item.label}
        </div>
        <div className="mt-4 text-xs font-semibold text-slate-500 uppercase tracking-widest">Level {item.level}</div>
      </div>
    ))}
  </div>
</div>

## Elevation Values

| Level | Value (dp) | Tailwind Class | Use Case |
| :--- | :--- | :--- | :--- |
| **0** | 0dp | `none` | Background, Wallpaper |
| **1** | 1dp | `shadow-sm` | Table rows, card outlines |
| **2** | 3dp | `shadow` | Standard Cards, Contained Buttons |
| **3** | 6dp | `shadow-md` | Dropdown Menus, Sticky Headers |
| **4** | 8dp | `shadow-lg` | Modals, Navigation Drawers |
| **5** | 12dp+ | `shadow-xl` | Large Dialogs |
